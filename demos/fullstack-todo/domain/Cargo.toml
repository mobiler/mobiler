[package]
name = "domain"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

# Self-contained (no workspace inheritance) so it's portable across the mobile +
# web workspaces that consume it.
[dependencies]
serde = { version = "1", features = ["derive"] }
